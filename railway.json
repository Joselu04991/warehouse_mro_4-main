{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "apt-get update && apt-get install -y tesseract-ocr tesseract-ocr-spa poppler-utils libgl1-mesa-glx libsm6 libxext6 libxrender-dev && pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "gunicorn --bind 0.0.0.0:$PORT wsgi:app",
    "healthcheckPath": "/api/warehouse/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
